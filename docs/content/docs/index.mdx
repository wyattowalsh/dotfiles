---
icon: index
title: docs
description: Operator index for the macOS/Linux rig SSOT.
---

Tracked state only. AI harness → [wyattowalsh/agents](https://github.com/wyattowalsh/agents). Map → [domain map](/docs/domain-map).

<DocsHub />

```bash
just --list
just check
just bootstrap --dry-run
just bootstrap-dev          # no args injects --dry-run
```

`just bootstrap` → Darwin `macos.sh` (dry-run default; `--apply` to mutate) or Linux `linux.sh` (mutates unless `--dry-run`). `just bootstrap-dev` always dry-runs when you pass no flags.

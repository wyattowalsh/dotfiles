---
icon: shell
title: Shell
description: Zsh structure + tools.
---

| File | Role |
| --- | --- |
| `rig/dots/zshrc` | → `~/.zshrc` |
| `rig/dots/p10k.zsh` | → `~/.p10k.zsh` |
| `rig/home/dot_zsh/functions/` | freshen, tests |
| `checks/zsh-structure.sh` | L0–L3 gate |

```bash
just check-zsh
just check-zsh-smoke    # local only
```

Structure rules + fixtures: `checks/fixtures/zshrc/`. Rollback: `checks/zsh-rollback-live.sh`. [freshen](/docs/freshen) · [Home](/docs/home-config).

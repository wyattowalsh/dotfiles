---
icon: ai-harness
title: AI
description: Harness lives in wyattowalsh/agents.
---

**SSOT:** [wyattowalsh/agents](https://github.com/wyattowalsh/agents) — MCP, skills, client configs.

Dotfiles only: env **names** (e.g. `MCPHUB_BEARER_TOKEN`, `BRAVE_API_KEY`) and Linux CLI/skills install via `setup.sh`. Values stay in `~/.zshrc.local` / private env — never git.

---
icon: home-config
title: Home
description: Chezmoi + rig/dots links.
---

| Surface | Path | Apply |
| --- | --- | --- |
| Runtime links | `rig/dots/*` → `$HOME` | bootstrap |
| Chezmoi | `rig/home/` | `chezmoi --source rig/home` |

```bash
just home-diff
just check-zsh          # dots ↔ home parity
```

Parity mirrors (`dot_zshrc.tmpl`, `dot_p10k.zsh`) are ignored by chezmoi apply — `cmp` only. Overrides: `~/.zshrc.local`, untracked includes. See [Shell](/docs/shell), [Terminal](/docs/terminal).

---
icon: freshen
title: freshen
description: Brew/mas upgrade helper.
---

`rig/home/dot_zsh/functions/freshen` → `~/.zsh/functions/` (Chezmoi).

| Mode | |
| --- | --- |
| default | inventory → upgrade → cleanup |
| `-N` / `--dry-run` | plan only |
| `-C` / `--clean-only` | cleanup path |
| `-S` / `--storage-plan` | report only |
| docker/gem prune | **opt-in** only |

```bash
just check-freshen
```

Tests: `rig/home/dot_zsh/functions/tests/`. Env knobs: `FRESHEN_*` (timeouts, low-power, opt-in reclaim) — see function header / tests.

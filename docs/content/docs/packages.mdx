---
icon: packages
title: Brew
description: Brewfile intent.
---

SSOT: `rig/brew/Brewfile`. Excludes: `rig/brew/exclude.txt`. Dumps under `local/` are evidence only.

```bash
just inventory-redacted
just brew-check
```

Promote curated names only. Docker Desktop cask is primary — not formula `docker`. Kopia CLI is `kopia/test-builds/kopia` (nightly job); `kopiaui` cask is manual restore only. See [Promote](/docs/ssot-workflow), [Backup](/docs/backup).

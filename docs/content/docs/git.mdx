---
title: Git
description: gitconfig + delta.
---

SSOT: `rig/dots/gitconfig` → `~/.gitconfig` (bootstrap link).

```bash
git config --global --list
```

Credentials / OAuth stay local. Identity overrides via local config / `includeIf`.

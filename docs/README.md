# Internal Docs

Fumadocs powers this internal documentation site.

```bash
task docs:install
task docs:build
```

The site is internal-facing and should describe tracked, validated state. Do not include private tokens, auth files, telemetry, histories, or app runtime databases.


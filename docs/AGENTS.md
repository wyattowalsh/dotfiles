# AGENTS

## Scope
Internal Fumadocs documentation site.

## Rules
- Keep docs factual and grounded in tracked manifests or verified local inventory.
- Use generated reference pages for package groups, AI/MCP clients, and validation commands when practical.
- Validate with `pnpm typecheck` and `pnpm build` when dependencies are installed.
- Do not publish private host details, tokens, or auth state.

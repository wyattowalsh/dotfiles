# AGENTS

## Scope
Chezmoi-managed dotfiles and machine-specific home configuration.

## Rules
- Track portable config and template machine-specific values.
- Keep identities, tokens, auth files, histories, caches, telemetry, and local app databases out of Git.
- Preserve `.local` and `.extra` override patterns for private customization.
- Preview with `chezmoi diff` before applying.


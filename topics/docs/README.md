# Docs Topic

Owns internal documentation workflow and Fumadocs content graph.

## Canonical path

`docs/` — Next.js + Fumadocs app with runbooks in `content/docs/`.

## Commands

```bash
just docs-install
just docs-build
just docs-ci
```

## When to update

Change MDX pages when justfile recipes, bootstrap behavior, or manifest policy changes. See `docs/AGENTS.md` for the sync matrix.

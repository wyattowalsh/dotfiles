# Security Topic

Owns no-secrets policy, secret scanning, private override rules, and safe import boundaries.

Secrets, auth files, histories, caches, telemetry, and local databases are never tracked.


name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6.1.0

      - name: Set up pnpm
        uses: pnpm/action-setup@v6.0.10
        with:
          version: 11.9.0

      - name: Set up Node.js
        uses: actions/setup-node@v6.5.0
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: docs/pnpm-lock.yaml

      - name: Install just
        uses: extractions/setup-just@v4
        with:
          just-version: 1.39.0

      - name: Set up system validation tools
        run: |
          sudo apt-get update
          sudo apt-get install -y zsh ripgrep shellcheck shfmt bats

      - name: Run CI validation
        run: just ci

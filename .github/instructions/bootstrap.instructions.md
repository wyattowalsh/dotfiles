---
applyTo: "bootstrap/**,setup.sh,checks/**,Taskfile.yml"
---

Preserve bootstrap idempotency. Support dry-run paths for mutating flows, keep shell scripts strict, and validate with `task check` plus focused smoke tests.

